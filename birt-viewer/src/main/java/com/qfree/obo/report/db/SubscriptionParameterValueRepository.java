package com.qfree.obo.report.db;

import java.util.UUID;

import org.springframework.data.jpa.repository.JpaRepository;

import com.qfree.obo.report.domain.SubscriptionParameterValue;

/**
 * Repository interface for {@link SubscriptionParameterValue} persistence.
 *
 * Only query methods that are generated by Spring Data JPA using Spring Data's
 * domain-specific language should be declared here.
 * 
 * @author Jeffrey Zelt
 */
public interface SubscriptionParameterValueRepository extends JpaRepository<SubscriptionParameterValue, UUID> {
	  
}
